node_modules/
package-lock.json
.env
*.log
npm-debug.log*
logs/
# Media store (vision build spec §2) — image bytes + meta, local per embodiment.
media/
# Vision capability cache (provider:model → yes/no) — advisory, rebuildable.
tomes/.vision-capability.json
tomes/.vision-capability.json.tmp
tomes/.memorization-queue.json
tomes/.memorization-queue.json.tmp
# Threat-tracker state (step 4b) — local, decaying scalar; never committed.
tomes/.threat-state.json
tomes/.threat-state.json.tmp
# Wait-streak experiment state (initiative-build-spec Pass 1) — local counter.
tomes/.wait-streak.json
tomes/.wait-streak.json.tmp
# Contact-baselines cache (initiative-build-spec Pass 2) — derived, local.
tomes/.contact-baselines.json
tomes/.contact-baselines.json.tmp
# Generated SearXNG config (random secret, per-boot port) — never committed.
tomes/.searxng/
# Outbox delivery queue (M11) — local pending notifications.
tomes/.outbox.json
tomes/.outbox.json.tmp
# Silence-triage last-activity stamp (M12b).
tomes/.last-activity.json
tomes/.last-activity.json.tmp
# Surface-events stream (slice 2) — per-embodiment record of offers + outcomes.
tomes/.surface-events.json
tomes/.surface-events.json.tmp
# Discord gateway runtime state — conversation map, rate-limit buckets, revisit queue.
tomes/.discord-map.json
tomes/.discord-map.json.tmp
tomes/.discord-revisits.json
tomes/.discord-revisits.json.tmp
tomes/.rate-limits.json
tomes/.rate-limits.json.tmp
# Google Calendar projection-cue aging state (§4).
tomes/.gcal-projection-cue.json
tomes/.gcal-projection-cue.json.tmp
# Google OAuth token store (refresh/access tokens — NEVER commit).
tomes/.gcal-google-token.json
tomes/.gcal-google-token.json.tmp
# Consent-pending tracking for the memorization greenlight flow.
tomes/.consent-pending.json
tomes/.consent-pending.json.tmp
# Village knock tracking — contact attempts from unregistered users/locations.
tomes/.village-knocks.json
tomes/.village-knocks.json.tmp
tomes/.village-location-knocks.json
tomes/.village-location-knocks.json.tmp
# User-generated tomes (Session Memories, Familiar's Ponderings, etc.)
# use UUID filenames. Shipped tomes use human-readable "*-Tome.json".
tomes/????????-????-????-????-????????????.json
tomes/????????-????-????-????-????????????.json.tmp
# Sample/test lorebook files dropped in tomes/ during development.
tomes/Sample*.json
.proto-familiar.pid
.proto-familiar-config.json
.proto-familiar.log.err
.pf-install-complete
settings.json
settings.json.tmp
packagebackup.json
# Per-install runtime data directories (never committed).
data/
lorebook/
memory/
# Windows shell metadata.
desktop.ini
**/desktop.ini
# Village registry local mirror (V1) — canonical copy lives in Phylactery.
village.json
village.json.tmp
.pf-backups/
# Claude Code agent worktree metadata (generated by the Agent tool).
.claude/
# CodeAlmanac local runtime state (job logs, index cache) — the wiki pages
# under almanac/ are tracked; only ephemeral CLI-generated artifacts are not.
almanac/.codealmanac/
almanac/*.db
almanac/*.db-journal

# Audio models — machine artifacts, fetched on demand, never committed (voice spec §0.1)
models/

# Reference voice clips (voice spec §6.5).
#
# voices/bundled/ IS committed — one clip, ~1.2 MB, the voice I have before my
# human has an opinion. It is in the repo rather than fetched so that a voice
# cannot go missing on a machine with no connection.
#
# The two below are not:
#   installed/  reproducible from a pinned url + checksum, so committing them
#               would mean every clone paying ~8 MB for choices almost nobody
#               makes.
#   ward/       clips my human gave me. This line is a privacy guard, not
#               housekeeping — such a clip may be someone else's copyrighted
#               performance, and personal use is theirs to make while
#               redistribution is not. It must never reach a commit.
voices/installed/
voices/ward/

# Temperature-probe renders — audition material, re-derivable by running the
# probe again, and pointless to carry in the repo.
voice-probe/

# Pin-time scratch (measuring what an archive unpacks to) — never committed
.pin-measure/

# Runtime state written by the running server (siblings of the tomes/.*.json above)
tomes/.memory-coverage.json
tomes/.memory-coverage.json.tmp
tomes/.stewardship-state.json
tomes/.stewardship-state.json.tmp

# Measured pitch of previewed reference clips — a cache, re-derivable by listening again
tomes/.voice-clip-features.json
tomes/.voice-clip-features.json.tmp

# Python bytecode — machine artifacts from any of the Python peers.
__pycache__/
*.pyc

# voicebox — Python venv and torch are machine artifacts, never committed
voicebox/.venv/
voicebox/uv.lock

# Session transcripts — generated from a Claude Code session log by
# `scripts/transcript-to-markdown.mjs`, for codealmanac to ingest locally.
# Not committed: they are megabytes of raw conversation that regenerate from
# the source log, and the durable version of what they contain belongs in the
# almanac and in docs/ rather than in a wall of chat.
docs/voice-development-transcript.md
docs/*-transcript.md
